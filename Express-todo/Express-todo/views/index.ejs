<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Express-Todo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous"/>
    <style>
      body {
        background-color: #f8f9fa;
        font-family: Arial, sans-serif;
      }

      h1 {
        font-size: 2rem;
        color: #343a40;
        margin-bottom: 1rem;
      }

      form input[type="text"] {
        padding: 0.5rem;
        font-size: 1rem;
      }

      form button[type="submit"] {
        padding: 0.5rem;
        font-size: 1rem;
        font-weight: bold;
      }

      .table {
        width: 100%;
        margin-top: 2rem;
      }

      .table th,
      .table td {
        text-align: center;
        vertical-align: middle;
      }

      .table th {
        background-color: #343a40;
        color: white;
      }

      .table a {
        text-decoration: none;
        color: white;
      }

      .table a.btn-danger {
        background-color: #dc3545;
      }

      .table a.btn-warning {
        background-color: #ffc107;
        color: black;
      }

      .table a.btn-warning:hover {
        background-color: #e0a800;
      }

      .table a.btn-danger:hover {
        background-color: #c82333;
      }
    </style>
  </head>
  <body>
    <div class="container mt-5">
      <h1 class="text-center mb-4">Form</h1>
      <form action="/showdata" method="post" class="mb-5">
        <div class="mb-3">
          <input
            type="text"
            name="id"
            class="form-control"
            placeholder="User Id"
            required
          />
        </div>
        <div class="mb-3">
          <input
            type="text"
            name="name"
            class="form-control"
            placeholder="User Name"
            required
          />
        </div>
        <div class="d-grid">
          <button type="submit" class="btn btn-primary">Submit</button>
        </div>
      </form>
      <table class="table table-striped table-bordered">
        <thead class="thead-dark">
          <tr>
            <th>No.</th>
            <th>Username</th>
            <th>Remove</th>
            <th>Edit</th>
          </tr>
        </thead>
        <tbody>
          <% studentdata.map((el,i)=>{%>
          <tr>
            <td><%= el.id %></td>
            <td><%= el.name %></td>
            <td>
              <a
                href="/deletedata?userid=<%=el.id %>"
                class="btn btn-danger btn-sm"
                >Delete</a>
            </td>
            <td>
              <a
                href="/editdata?editid=<%=el.id %>"
                class="btn btn-warning btn-sm"
                >Edit</a>
            </td>
          </tr>
          <%})%>
        </tbody>
      </table>
    </div>
  </body>
</html>
